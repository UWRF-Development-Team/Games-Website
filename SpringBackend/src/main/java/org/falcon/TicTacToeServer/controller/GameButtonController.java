package org.falcon.TicTacToeServer.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;

@Controller
public class GameButtonController {
    //@GetMapping("/tictactoe/gamebutton")
     /* A game button is rendered to the user with a phrase and (with event
     listeners) an interaction with the server. Let's break down what the
     possible interactions are:
     -------------------------------------------------------------------------
     1. User click: the board performs a clearBoard() if the game is over.
     (We can know it's over by checking if the board is frozen [a state we
     can set in a data structure like a class]).

      */

}
